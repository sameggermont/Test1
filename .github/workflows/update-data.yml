name: Update game data

on:
  workflow_dispatch:      # lets you run it manually from the Actions tab
  schedule:
    - cron: "0 6 * * 1"   # every Monday at 06:00 UTC, keeps rankings fresh

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Fetch top 500 games from BoardGameGeek
        run: python3 scripts/fetch_games.py

      - name: Commit updated data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/data/games.json
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Update game data from BoardGameGeek"
            git push
          fi
